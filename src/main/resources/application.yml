spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cryptox_db
    username: root
    password: Yogesh445#

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    open-in-view: false

    properties:
      hibernate:
        format_sql: true
